# Third-Party Notice

The dashboard optionally loads SheetJS Community Edition (`xlsx`) from jsDelivr for client-side Excel import/export.

Project: https://sheetjs.com/
Package: `xlsx@0.18.5`

The dashboard code itself does not bundle the third-party library; the browser requests it from the CDN at runtime.
